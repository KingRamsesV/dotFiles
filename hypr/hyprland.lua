
require("config/variables")
require("config/settings") -- Environment variables, startup settings, general hyprland settings, etc.
require("config/monitors") -- Monitor config - layout & resolution stuff
require("config/animations") -- animations config mostly copied from other places
require("config/binds") -- Keybinds\nrequire("config/rules") -- Window Rules - borders and styling stuff that aren't animations
